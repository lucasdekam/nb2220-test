image: python:latest

before_script:
  - pip install jupyter-book

pages:
  stage: deploy
  script:
  - jupyter-book build .
  artifacts:
    paths:
    - _build/html
    expire_in: 1 week
  only:
  - master
